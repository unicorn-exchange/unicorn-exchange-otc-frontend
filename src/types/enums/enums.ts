export enum UserTypes {
  buyer,
  guest,
  creator,
}

export enum Statuses {
  ready,
  waiting,
}

export enum DealProgresses {
  inProgress,
  done,
}

export enum DealTypes {
  create,
  buy,
}

export enum Headers {
  Authorization = "Authorization",
  TestEnv = "TEST_ENV",
  TestEnvPassword = "TEST_ENV_PASSWORD",
}
